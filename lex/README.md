# Lex product layer

Fork-only assets that should stay out of upstream paths when possible.

| Path | Contents |
|------|----------|
| `presets/` | Operator INI presets (safe local + router examples) |
| `scripts/` | Baseline bench helpers and DX scripts |

Product documentation lives in [`docs/lex/`](../docs/lex/README.md).
