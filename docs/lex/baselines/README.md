# Baseline artifacts

Store measured `llama-bench` CSV files and short notes here.

Suggested names:

```text
bench-cpu-ref-<model>-<quant>-<yyyymmdd>.csv
```

Keep [SUMMARY.md](SUMMARY.md) updated with the latest accepted numbers.
